services:
  broker:
    image: cursus-broker:latest
    build:
      context: ..
      dockerfile: Dockerfile
    ports:
      - "9000:9000"
    environment:
      - CURSUS_BROKER_PORT=9000
    volumes:
      - ./data:/data
