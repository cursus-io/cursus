# Consumer
broker_addrs:  
  - "broker:9000" 
log_level: "info" # debug, info, warn, error

topic: "bench-topic"
group_id: "test-consumer-group-1"
consumer_id: "consumer-1"

# Consumption
mode: "polling" # polling, streaming
poll_interval: 100ms
poll_timeout_ms: 3000
batch_size: 10000
max_poll_records: 10000

# Timeout
session_timeout_ms: 30000
streaming_read_deadline_ms: 10000

# Benchmark, Validation
enable_benchmark: true
enable_correctness: true
num_messages: 100000
