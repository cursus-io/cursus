# Consumer
broker_addrs:  
  - "broker-1:9000"
  - "broker-2:9000"
  - "broker-3:9000"
log_level: "info"

topic: "bench-topic"
group_id: "cluster-consumer-group"
consumer_id: "cluster-consumer-1"

mode: "polling"
poll_interval: 100ms
poll_timeout_ms: 5000
batch_size: 5000

session_timeout_ms: 30000
enable_benchmark: true
enable_correctness: true
num_messages: 100000
