{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "cursus.serviceAccountName" . }}
  labels:
    {{- include "cursus.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
